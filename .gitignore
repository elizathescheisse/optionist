# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Demo screenshots — committed to git as a visual history of each PR
# screenshots/

# Seed data — place an Optionist export here to pre-load data on first launch.
# Gitignored by default so private design work isn't accidentally committed.
# See README.md for the sharing workflow.
public/seed.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
