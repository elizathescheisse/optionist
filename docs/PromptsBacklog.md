# Prompts Backlog

- Can you make a new branch to explore: "Right panel as document, not form — changing labels/sections to horizontal rules, 'Goal' instead of 'Description' — low effort but shifts the feel meaningfully."
- Sidebar card redesign — the "editorial object" direction is appealing but it's a big enough redesign that it deserves its own focused session, not a polish pass.
- Selective UX integration (`integrate-from-ux` branch) — walk through the friend's PR additions one at a time and pull in only what's wanted. We never started; the first one to understand was `plans/optionist-ux-foundation.md`. That's still queued. Note: the `UX` local branch holds the friend's full PR as reference, but it's *behind* its remote (friend pushed newer commits not yet pulled).
